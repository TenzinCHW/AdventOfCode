// Quite easy but need to find out how to store lamdas and also need to construct a lambda
// HashMap of lambdas that return either A, R or the name of the next lambda
// Process each MachinePart by passing it into the first lambda

struct MachinePart {
    x: u32,
    m: u32,
    a: u32,
    s: u32,
}
